import { p as polka, a as applyHandler } from './apply-handler-c4b575e3.js';
import 'http';
import 'querystring';
import '$handler_file';

const server = polka();

applyHandler(server);

var vercelHandler = (req, res) => server.handler(req, res);

export { vercelHandler as default };
